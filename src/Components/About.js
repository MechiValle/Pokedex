import React from "react";

const About = () => {
    
    return(
            <div className="about">
                Pokedex created with Create React App.
            </div>
    );
};

export default About;